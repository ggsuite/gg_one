name: gg_one
description: A collection of checks and actions like analyzing, linting, testing, code coverage, committing, pushing, publishing.
version: 10.3.0
publish_to: none
repository: https://github.com/ggsuite/gg_one.git

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  args: ^2.7.0
  gg_args: ^2.0.4
  gg_console_colors: ^2.1.5
  gg_git: ^3.1.0
  gg_is_flutter: ^1.0.7
  gg_lang:
    git:
      url: https://github.com/ggsuite/gg_lang.git
      ref: publish_via_pr_by_default
  gg_log: ^1.0.6
  gg_process: ^1.1.6
  gg_publish:
    git:
      url: git@github.com:ggsuite/gg_publish.git
      ref: publish_via_pr_by_default
  gg_status_printer: ^1.1.4
  gg_test:
    git:
      url: git@github.com:ggsuite/gg_test.git
      ref: publish_via_pr_by_default
  gg_version:
    git:
      url: git@github.com:ggsuite/gg_version.git
      ref: publish_via_pr_by_default
  meta: ^1.18.2
  mocktail: ^1.0.5
  path: ^1.9.1
  pub_semver: ^2.2.0
  recase: ^4.1.0
  yaml: ^3.1.3
  yaml_edit: ^2.2.4
  gg_capture_print: ^1.0.9
  gg_changelog: ^1.1.0
  gg_is_github: ^1.0.6
  matcher: ^0.12.20
  gg_direct_json: ^2.0.1
  gg_merge:
    git:
      url: git@github.com:ggsuite/gg_merge.git
      ref: publish_via_pr_by_default
  interact: ^2.2.0

dev_dependencies:
  fake_async: ^1.3.3
  lints: ^6.1.0
  test: ^1.31.1

executables:
  gg_one: